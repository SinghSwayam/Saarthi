name: saarthi
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.7.2 <4.0.0" # Ensure compatibility

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.6 # GetX for state management and navigation
  shared_preferences: ^2.2.2 # For storing user authentication state
  http: ^0.13.6 # API calls (ensure compatibility with Dart version)
  connectivity_plus: ^5.0.2 # To check internet connectivity (optional)
  flutter_secure_storage: ^9.0.0 # Securely store tokens (if needed)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/splash.png
    - assets/images/background.png
